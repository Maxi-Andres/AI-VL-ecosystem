para poder usar yolo
source ".venv/bin/activate   # la consola va a mostrar (.venv)
deactivate                   # salis de el venv

To do:

- poner una base de datos para que la ia pueda tener mas contexto o cosas para las prompts

nueva tab para controlar movimiento de robot como hace el shoystick

despues en la version final hay que implementar que solo este el modo de voz y que el sepa elegir si es una accion o si es una pregunta, y si tiene o no que usar las imagenes de la camara, ya sea para una accion o para una pregunta.

mejorar el tts es una chota

en command_common.py meter los examples en español, creo que eso es lo que usa para determinal la skill por que no le esta pegando mucho la verdad 

se puede saber la cantidad de bateria?

que se pueda cambiar la resolucion de el video de el robot y los fps

que se pueda manejar desde la app tipo los sticks y que se pueda desplegar una lista de las accciones que puede hacer en el front y que las puedas tocar de una y se haga, eso depende de el robot que selecciones tienen que cambiar

no anda lo de que si le decis "camina para adelante un segundo" lo haga por un segundo, sigue caminando indefinidamente

que el SELECCIONAR el robot que estas usando sea mas obvio

que tenga una pill arriba que ponga si el robot esta conectado tipo robot online, y podria incluso el disernir cual es o algo asi

cambiar la estructura de vlm por que no se entiende mucho

ver como implementar el microfono y los parlantes de el robot, ver todas las opciones que tiene el robot basicamente, (poder hacer lo mismo de la app de unitree pero en esto asi no hace falta usarla)

rtsp? usar algun protocolo o algo para que sea mas rapido

CUANDO ESTA EL SAFE OFFF TIENE QUE ESTAR EN ROJO, y cuando esta on, te tiene que poner los botones peligrosos en diabled

te tiene que dejar elegir el robot y cambiar los botones, que el robot se cambie globalemente desde el header, corregir lo de la pill eso pone conected cuando muestra la camara de live

sacar los frames van a el back para despues lo de yolo, , o que en el drive sean directos los frames sin latencia

anda como el orto el view only arreglalo

agente de splunk y agente de thousand eyes

LA ip que agarra por wireless no es la misma que cuando me conecto por cable? por que son dos computadoras diferentes??

verifica que funcionen todas las opciones que tienen para ejecutar ambos robots

IMPORTANTE, CUANDO INICIA LA APP SE FIJA LA IP 123.161, ES LA bajo nivel de ambos robots, quiza intenta filtar por alto nivel? o algo asi

que en el front tenga todas las fucniones que tiene la app poronga de unitree, tenes armado el watcher

hay acciones que siguen sin funcionar fijate de arreglarlas y que ande todo

--------------------------------------------------------------

Done:
- !IMPORTANTE para la foto de las chicas tenes que poner esto, si no se queda sin contexto y no da output, pero tarda mucho (30segs) para mi va a haber que hacer muchos modelos uno para personas por ejemplo, uno para objetos, quiza que lo identifique con el prompt de voz que le das que ahi elija cual usar 
  "max_tokens": 2048,
  "num_ctx": 8192,

- !FIJATE que los cuadros sean de distintos colores como en las pruebas por que los hace todos verdes, y que en la compu veas la transmision de la camara, quiza que cuando entres te deje elegir entre ser admin/monitor y transmisor, que el transmisor solo vea el video que transmite
y el monitor que pueda ver el video y que tenga todas las opciones

- intentar evitar que el modelo piense, quiza fijate otra version o en hugging face, el │ qwen3-vl:4b │ piensa siempre aunque lo saques
- proba con ollama en consola de una si te permite sacar el think NO, piensa igual siempre se necesita el modelo no think

- !FIJATE de mas adelante cuando este el robot y tenga que pensar mas si quiza combiene alternar entre ambos modelos, el instruct y el thinking dependiendo la tarea, ademas de usar yolo 

- !FIJATE de probar de meter un VLA en el medio o directamente sacar el VLM investiga eso (igual en esta compu los dos no pueden correr)

- creo que hay un cuello de botella, cuando analizas una foto como que tarda mas, pero despues si haces la segunda tarda menos
como que tarda el primer intento por que le cargas el modelo a la GPU? se podra implementar algo para que este siempre cargado? 
o que pueda definir si pre-cargarlo para despues usarlo por que necesito los menores tiempos posibles, por que necesito velocidad cuando este implementado el robot
ademas me di cuenta que cuando hago 2 veces la misma imagen la segunda vez tambien es mas rapido
el tiempo desde que toco enter hasta que veo como imprime lo que piensa y tambien lo que piensa en total es menos cuando repito la imagen

- dale accesso a claude para que pruebe con distintos prompts, max_tokens, etc, para intentar reducir el tiempo al maximo 

DONE - que los benchmarks se guarden en distintos archivos que no se pisen entre ellos si no no se puede comparar, y que en estos ademas de lo que se guarda tambien se guarden los jsons de cada imagen que detectan para poder ver que pusieron, verificar tambien que la ia no lea los nombres de las imagenes, eso es trampa

- hace un front real para no usar mas la consola fijate si podes hacer que python sea el back end

- !FIJATE de provar que onda YOLOv8 o v11 quiza son mas rapidos o podes usar eso para hacer rapido y despues los vl para el movimiento de el robot y las respuestas a comandos de voz

- que este la mejor config y la actual, y una manera de pasar la actual a la mejor, luego de pruebas y tocar cosas, y tambien una manera de guardar la mejor si hay una mejor
- hay que normalizar las imagenes? o quiza bajar la resolucion?

- !FIJATE pensa esto, no tiene sentido analizar fotograma por fotograma de el video, lo que se puede hacer es que YOLO corra todos los frames para ver que realmente haya algo (ej: de 30 frames en 28 detecto una botella) y hacer el promedio de si detecto bien, y despues de 30 en 1 corres Qwen3 para tomar decisiones complejas y preguntas mas piolas (yolo detecta 2 botellas y despues yo le pregunto a qwen el color por ejemplo)

- en el tiempo tenes que considerar la primera deteccion que tarda mas por que se carga el modelo en vram

cuando le decis interpret es mucho mas rapida, eso debe ser por que no manda imagen
tiene que poder dicernir si analizar la imagen o hacer un movimiento, 

hace las cosas para el perro men no solo para el humano, aparte el perro es mas facil de levantar y usar

que se pueda poner para que ejecute los comandos solo tipo switch on off y que se pueda tambien switch para que ejecute los comandos de una o que tengas que tocar el boton para activarlos

en command common poner eejemplos en español de que tiene que hacer por que le se equivoca bastante

que se pueda tambien poner pantalla completa en monitor, no se por que no tiene la opcion, tienen que ser lo mismo 

dividir en dos las opciones por que es muy largo ya que cambies de pagina, yolo, vlm, commands y control (nuevo EN OTRA TAB DIRECTAMENTE)

que se pueda agrandar la pantalla de el robot tambien a pantalla completa y que sea un poco mas chica

que se pueda activar y desactivar yolo tambien con un switch y que sea para el el que sea que sea el video, ya sea live o el video de el robot


------------------------------------------------------------------------------------------------------------------------------------------------

1. Resolución del VLM

- Desde el celu (Live): sí, resolución full. Tanto el botón normal como el prompt libre usan captureFrame(video), que toma el frame a la resolución nativa de la cámara (calidad 0.85), no el frame reducido del streaming.
- Desde el monitor del server: NO. El monitor solo tiene el frame espejado (~`imgsz`, chico), así que el VLM ahí usa esa resolución baja. Para que el monitor también use full habría que pedirle al celu una captura hi-res puntual (round-trip). Te lo dejo como opción si lo querés (no lo metí para no cambiar el ancho de banda del mirror).

------------------------------------------------------------------------------------------------------------------------------------------------

=====================================================================================
Model        imgsz  conf     P50     P95    Mean     Min     Max    Total  ~obj  Errs
-------------------------------------------------------------------------------------
yolov8n.pt     640  0.25    15ms    15ms    15ms    15ms    15ms     15ms  10.0     0
yolov8s.pt     640  0.25    15ms    15ms    15ms    15ms    15ms     15ms  17.0     0
yolo11n.pt     640  0.25    15ms    15ms    15ms    15ms    15ms     15ms  16.0     0
yolo11s.pt     640  0.25    16ms    16ms    16ms    16ms    16ms     16ms  20.0     0
yolo11m.pt     640  0.25    18ms    18ms    18ms    18ms    18ms     18ms  14.0     0
yolo11l.pt     640  0.25    20ms    20ms    20ms    20ms    20ms     20ms  17.0     0
yolo11x.pt     640  0.25    23ms    23ms    23ms    23ms    23ms     23ms  17.0     0
yolov8m.pt     640  0.25    18ms    18ms    18ms    18ms    18ms     18ms  20.0     0
yolov8l.pt     640  0.25    22ms    22ms    22ms    22ms    22ms     22ms  19.0     0
yolov8x.pt     640  0.25    25ms    25ms    25ms    25ms    25ms     25ms  20.0     0
=====================================================================================
MAS DETECCIONES NO SIGNIFICA MEJOR, PUEDE DAR FALSOS POSITIVOS
fijate las fotos pero para mi esta buena yolo11s, lo que no probe es cambiar el tamaño de la imagen, mas grande peor pero mas preciso
las clases de COCO no afectan a la velocidad por que hace single-shot
------------------------------------------------------------------------------------------------------------------------------------------------

- Caché de Ollama: la 1ª vez que ve una imagen hace el prefill completo (~9s); si repetís la misma imagen (o ya la vio en un combo anterior), devuelve un cache-hit en ~1s. Los "1s" que vi al principio eran imágenes ya cacheadas, no nuevas.
- Recarga del modelo: cada vez que cambia num_ctx, Ollama recarga el modelo (~10s), y ese costo caía sobre la 1ª llamada de cada combo, inflando las mediciones.

Qué hace cada parámetro (real)

- max_tokens: irrelevante. El instruct devuelve ~75 tokens y corta solo (finish_reason: stop); nunca toca el tope. Lo dejé en 512. (ESTO INFLUYE PARA CUANDO SE CORTABA EL THINK MODEL POR MUCHO OUTPUT, EN LOS MODELOS QUE NO PIENSAN NO IMPORTA)
- prompt (v3_minimal vs v1): misma velocidad y misma calidad. El prefill lo dominan los tokens de la imagen, no las ~150 palabras del prompt. Dejé v3_minimal igual (es más limpio).
- num_ctx: no cambia la velocidad entre 2048–4096, pero imágenes grandes (ej. 18.jpeg) dan error 400 por debajo de 3072. → conviene 4096 (entra todo, 100% en GPU).
- El verdadero acelerón ya estaba aplicado: usar instruct en vez de thinking (~9s vs ~18s).

Config que dejé (la más rápida y robusta)

model=qwen3-vl:4b-instruct, variant=v3_minimal, num_ctx=4096, max_tokens=512, think=false (en smoke y benchmark).

Para bajar de ~9s de verdad (próximos levers, no estos knobs)

1. Mantener el modelo caliente (keep_alive en Ollama) y no cambiar num_ctx → te ahorrás la recarga de ~10s.
2. Pre-reducir la resolución de las imágenes antes de mandarlas (menos tokens de visión = menos prefill). Esto sí atacaría los ~9s, pero requiere agregar redimensionado (p. ej. Pillow) a encode_image.
3. GPU con más VRAM / otra cuantización.

Si querés, implemento el redimensionado de imágenes (lever #2) y lo benchmarkeo para ver cuánto baja — es la opción con más chance de reducir el tiempo real. Como siempre, no commiteé nada; quedan todos los cambios para tu revisión.

------------------------------------------------------------------------------------------------------------------------------------------------

Diferencia entre max_tokens y num_ctx

- num_ctx = la ventana de contexto completa: todo lo que entra (system + prompt + tokens de la imagen) + todo lo que sale (razonamiento + respuesta). Es el número que ves en ollama ps. Más num_ctx → la imagen entra a más resolución (más detalle) pero el prefill es más lento. Por eso "más contexto = más lento".
- max_tokens (= num_predict) = el tope de lo que el modelo genera (razonamiento + respuesta). Si lo alcanza, corta → finish_reason: length y el content puede venir vacío.
- Cómo se combinan: la salida real disponible es min(max_tokens, num_ctx − tokens_de_entrada). Los dos tienen que alcanzar: si max_tokens es chico corta aunque sobre num_ctx; si num_ctx es chico, la imagen le come lugar a la respuesta y también se traba. Por eso "poco contexto se traba, mucho contexto va lento" — hay que encontrar el punto medio (8192 / 4096 funcionó bien acá).

------------------------------------------------------------------------------------------------------------------------------------------------

⚠️ Lo que hay que confirmar al revisar

Las claves del JSON ahora son inglés (objects/type/…). Eso es el contrato VLM→VLA que consume el "Silk AI Proxy Gateway / F1.9": el consumidor downstream tiene que actualizarse para leer las claves nuevas, si no, se rompe. Lo dejé anotado en CLAUDE.md también. Como vos revisás antes de usar/commitear, lo verificás ahí.
