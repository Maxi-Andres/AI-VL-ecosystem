# =============================================================================
#  Wrapper repo: holds only the launchers (win/, linux/) and the docs.
#  The three app repos are INDEPENDENT (each its own .git) and are NOT part of
#  this repo.
# =============================================================================

# --- App repos (each is its own git repository) ------------------------------
AI-VL-core/
AI-VL-backend/
AI-VL-frontend/

# --- Local TLS certificates --------------------------------------------------
# Generated per machine/IP by win/run.bat. The private key (key.pem) must NEVER
# be pushed to git; they also depend on the IP, so versioning them is pointless.
certs/

# --- codebase-memory reindex cache (per-machine signatures) ------------------
.claude/.reindex-state/

# --- OS / editor cruft -------------------------------------------------------
Thumbs.db
Desktop.ini
.DS_Store
*.swp
*~
.vscode/
.idea/
